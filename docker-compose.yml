services:
  argus:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      ARGUS_DB_PATH: /app/data/argus.db
      PORT: "8000"
    volumes:
      - ./data:/app/data
    init: true
